spring:
  application:
    name: money-manager
  datasource:
    url: jdbc:postgresql://db.usjlhzwgmfwudgnqowhm.supabase.co:5432/postgres
    username: postgres
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
